# Personal site archive — 2026-09-06

This directory preserves the previous personal-site implementation and its static assets.

It is intentionally outside `src/app` and `public`, so none of its pages or assets are exposed by the current Next.js application. The current site starts from the minimal files in `src/app`.
